clear all; close all; clc;

% -------------------------------------------------------------------------
% 二维高斯函数
x = -10:0.1:10;
y = -10:0.1:10;
sigma = 3;
[xx, yy] = meshgrid( x, y);
z1 = exp(-(xx.^2 + yy.^2)/(2*sigma^2)) / (2*pi*sigma^2);
mesh(x, y, z1);

title('二维高斯分布曲线');


